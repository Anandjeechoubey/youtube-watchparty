# Production relay URL. Copy this file to ".env" and set your deployed relay.
# ./build-prod.sh bakes this into the built extension and hides the Server URL
# field from the UI.
SERVER_URL=wss://your-relay.workers.dev
